## backend
    You will need:
    1. pip install flask
    2. pip install jwt( till now)
    
    Firebase account:
        - firebase sdk
        - pip install firebase-admin

## Frontend
    You will need:
    1.react